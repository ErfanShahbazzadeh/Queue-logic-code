class queuecalss:
    def __init__(self):
        front = 0
        rear = 0
        Queue = list()

    def Enqueue(self,object):
        if IsEmpty() == True:
            self.Queue[self.rear]  = object

    def Dequeue(self):
        if IsFull() == True:
            return object
    
    def Peek(self):
        return object

    def ReverseQueue():
        return queue

    def IsEmpty(): 
        return boolean

    def IsFull(): 
        return boolean